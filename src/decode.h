#ifndef NECO_DECODE_H
#define NECO_DECODE_H

#include "file.h"
#include "wav.h"
#include "error.h"

#define inline __inline__
#define likely(expr) __builtin_expect(expr, 1)
#define unlikely(expr) __builtin_expect(expr, 0)

#endif
